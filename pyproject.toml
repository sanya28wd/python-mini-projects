[project]
name = "python-mini-projects"
version = "1.0.0"
description = "A curated collection of small Python games and applications."
requires-python = ">=3.11"
dependencies = []

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "I", "UP"]
